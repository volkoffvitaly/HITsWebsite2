@model hitsWebsite.Models.ManageViewModels.IndexViewModel
@using hitsWebsite.Views.Manage
@{
    ViewData["Title"] = "Профиль";
    ViewData.AddActivePage(hitsWebsite.Views.Manage.ManageNavPages.Index);
}

<h4>@ViewData["Title"]</h4>
<b>Имя пользователя:</b> @Html.DisplayFor(model => model.Username)

@section Scripts {
    @await Html.PartialAsync("_ValidationScriptsPartial")
}
